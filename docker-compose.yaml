services:
  postgres:
    image: postgres
    container_name: passaaqui_postgres

    restart: always

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: passaaqui

    ports:
      - "5432:5432"
  # jdbc:postgresql://localhost:5432/passaaqui?user=postgres&password=postgres
